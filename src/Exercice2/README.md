# Exercice 2: composition using the render props pattern (special "children" props)

Refactor the `Bart`, `Albatros` and `Interview` components by using the render props pattern with the special "children" props.
